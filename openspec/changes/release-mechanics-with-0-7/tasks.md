## 1. Red first

- [ ] 1.1 Add `test_the_manual_states_the_release`, `test_the_manual_links_existing_framework_pages` and `test_the_reference_names_no_consumer_projects` to `tests/test_documentation.py` and record them failing on the current text.

## 2. Narrative and links

- [ ] 2.1 Rewrite the release note on `docs/index.rst` and the installation page: released 0.1.0 with Machinome 0.7.0, index install first, source build for contributors, migration note pointing at the framework's upgrading page.
- [ ] 2.2 Repoint every framework link in `docs/` to a page the 0.7 manual has.
- [ ] 2.3 Replace the `solid2` passages in `docs/using-with-machinome.rst` and `docs/reference/indexing.rst` with the framework's spellings.

## 3. Reference without project credits

- [ ] 3.1 Describe motivating machines by kind on the gears, screws, cranks, cams, indexing, deltas, linkages, rolling and belts pages, keeping every value, sign and domain statement.

## 4. README, changelog and records

- [ ] 4.1 Rewrite the README's status and installation sections and the helper count.
- [ ] 4.2 Write the 0.1.0 release section of `CHANGELOG.md`.
- [ ] 4.3 Update `workflow/release-0.1.md`, `workflow/README.md`, `workflow/documentation.md` and `openspec/config.yaml` to the released state.

## 5. Evidence

- [ ] 5.1 Run the documentation and formula suites, the strict HTML and doctest builds, the distribution check, and check every framework link against the built Machinome 0.7 manual; record the results in `evidence.md`.
- [ ] 5.2 Sync the baseline specs and archive the change.
